{{#Dialog open=isOpen}}
<form method="dialog">
    <h4>Смена пароля</h4>
    {{{ FormField label="Старый пароль" name="old_password" ref="oldPassword" isActive="true" type="password"}}}
    {{{ FormField label="Новый пароль" name="new_password" ref="newPassword" onChange=onChange isActive="true" type="password"}}}
    {{{ FormField label="Повторите пароль" name="new_password_confirm" ref="newPasswordConfirm" onChange=onChange isActive="true" type="password"}}}
    {{{ ErrorMessage error=error ref="errorMessage"}}}
    <div>
        {{{ Button
        label="Изменить пароль"
        type="submit"
        style="primary"
        onClick=onSubmit
        }}}
        {{{ Button
        label="Отменить"
        type="button"
        style="secondary"
        onClick=onClose
        }}}
    </div>
</form>
{{/Dialog}}
