{{#Dialog open=isOpen}}
<form method="dialog">
    <h4>Удалить пользователя из чата</h4>
    {{{ FormField label="ID пользователя" name="remove_user_id" ref="removeUserID" isActive="true"}}}
    {{{ ErrorMessage error=error ref="errorMessage"}}}
    <div>
        {{{ Button
        label="Удалить"
        type="submit"
        style="primary"
        onClick=onRemoveUser
        }}}
        {{{ Button
        label="Отменить действие"
        type="button"
        style="secondary"
        onClick=onClose
        }}}
    </div>
</form>
{{/Dialog}}
